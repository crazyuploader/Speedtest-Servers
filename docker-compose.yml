services:
  speedtest_servers:
    image: crazyuploader/speedtest-dashboard
    volumes:
      - ./data:/app/data:ro
    ports:
      - "${SPEEDTEST_INTERFACE:-127.0.0.1}:8202:8202"
    restart: unless-stopped
    logging:
      options:
        max-size: 256m
